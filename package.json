{
  "name": "pulkka-pwa",
  "version": "0.0.0",
  "description": "A simple PWA app for clocking in and out of work.",
  "scripts": {
    "dev": "npx vite",
    "preview": "npx serve www",
    "e2e": "playwright test",
    "e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/node": "^22.19.17"
  }
}
